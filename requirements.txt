# Core server
fastapi==0.119.0
uvicorn==0.37.0

# Async HTTP client (used for reCAPTCHA verification)
aiohttp==3.10.11

# Supabase client
supabase==2.5.0

# Environment variable loader
python-dotenv==1.0.1

# CORS middleware (included via Starlette but we pin it)
starlette==0.48.0

# Optional but recommended for better JSON handling and security
pydantic==2.12.0
httpx==0.28.1
python-multipart==0.0.20
